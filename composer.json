{
    "name": "nosh/omnitax",
    "description": "OmniTax — a Laravel package for Pakistan's fiscal (digital) invoicing. It connects a business's system to its tax authority in real time to obtain an official fiscal invoice number and QR code. Built for Pakistan's FBR and tax system: it currently ships FBR (federal / PRAL) integration, with provincial (PRA, SRB, KPRA, BRA) and further tax-authority integrations planned. Authority-driven, multi-tenant, and extensible to more authorities by design. A Nosh (RapidFlow) package.",
    "type": "library",
    "license": "MIT",
    "keywords": ["laravel", "omnitax", "fbr", "pral", "digital-invoicing", "fiscal", "pakistan", "pra", "srb", "kpra", "bra", "e-invoice", "tax"],
    "authors": [
        { "name": "Nosh", "homepage": "https://noshpk.com" }
    ],
    "require": {
        "php": "^8.1",
        "ext-json": "*",
        "illuminate/support": "^10.0 || ^11.0 || ^12.0 || ^13.0",
        "illuminate/database": "^10.0 || ^11.0 || ^12.0 || ^13.0",
        "illuminate/bus": "^10.0 || ^11.0 || ^12.0 || ^13.0",
        "illuminate/queue": "^10.0 || ^11.0 || ^12.0 || ^13.0"
    },
    "require-dev": {
        "phpunit/phpunit": "^10.0 || ^11.0",
        "orchestra/testbench": "^8.0 || ^9.0 || ^10.0 || ^11.0"
    },
    "suggest": {
        "bacon/bacon-qr-code": "Render QR codes to PNG/SVG at the authority-required size (^2.0 or ^3.0). Payload/dataUri work without it."
    },
    "autoload": {
        "psr-4": {
            "Nosh\\OmniTax\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "Nosh\\OmniTax\\Tests\\": "tests/"
        }
    },
    "extra": {
        "laravel": {
            "providers": [
                "Nosh\\OmniTax\\OmniTaxServiceProvider"
            ],
            "aliases": {
                "OmniTax": "Nosh\\OmniTax\\Facades\\OmniTax"
            }
        }
    },
    "config": {
        "sort-packages": true
    },
    "minimum-stability": "stable",
    "prefer-stable": true
}
